# T011/T012 任務完成摘要（digital-twin）

- 日期：2026-08-05
- 專案：~/Projects/digital-twin（程式碼）
- 執行者：OpenCode with DeepSeek V4 Flash

## T011 - 統一模型與路徑設定模組 (config.py) ✅

**問題**：`multi_ai_discuss.py`、`auto_develop.py`、`README.md` 三處模型定義互相矛盾（同一「claude」在三處是三個不同 model_id，且名稱與實際模型不符）。

**修改**：
- 新增 `~/Projects/digital-twin/config.py`：單一設定來源，含模型清單（nemotron/gemini/deepseek/grok，各含 api_env/enabled/manual 標記）、DEFAULT_IMPL_MODEL
- `multi_ai_discuss.py`：改用 config 的模型定義（2 blocks）
- `auto_develop.py`：移除 L605-607 重複硬編碼，改用 config DEFAULT_IMPL_MODEL
- `consensus_eval.py`：改用 config 的 MODELS
- `spec_auto_merge.py`：MODEL_NAMES 改為 ['nemotron','gemini','deepseek','grok']

**驗證**：
- `python3 multi_ai_discuss.py --list-models`：nemotron/gemini/deepseek ✅（有 Key）、grok ⚠️ 手動
- `python3 auto_develop.py --list`：任務清單正常顯示
- 環境注意：需用 `/opt/homebrew/bin/python3`（Python 3.14.6，有 dotenv+yaml）；QClaw python 3.11.10 缺 dotenv

## T012 - 品質閘門分層（只檢查 diff 檔案）✅

**問題**：`run_tests()` 閘門失真——ruff 掃全專案有 333 個既有錯誤、無 tests/ 目錄（pytest rc=5 被當通過）、pyright include 指向不存在的 src/。導致閘門太鬆或太嚴兩極。

**修改**（`auto_develop.py`）：
- `run_tests(code_dir, changed_files)` 分層：
  - L1 ruff 只檢查本次變更檔案（`git diff --name-only` + `git ls-files --others`）；無變更清單時退路只查致命錯誤（E9/F821），不被 style 舊債誤擋
  - L2 pyright（若安裝）
  - L3 pytest：rc=5（無測試）明確印「⚠️ 無測試可執行，pytest 跳過（rc=5）」，不視為失敗
- 新增 `_get_changed_files()` 方法

**驗證**：
- 語法錯誤（invalid-syntax）→ 正確擋下 ✅
- 乾淨檔案 → 全閘門通過 ✅
- 無變更清單 → E9/F821 全專案通過（不被 333 舊債擋）✅

## 順手修復（T024 - twin Python 解析器）✅

- `twin` CLI 的 `PYTHON_BIN = sys.executable` 指向 QClaw python（無 dotenv）→ `./twin status` 顯示「無 Key」假象
- 新增 `_resolve_python()`：優先使用有 dotenv/yaml 依賴的直譯器，依序嘗試 `/opt/homebrew/bin/python3`、`/usr/local/bin/python3`、`/usr/bin/python3`
- 修正 `spec_auto_merge.py` version 前綴 bug（`f"v{version}"` 造成 vv2），現接受 `v2` 或 `2`
- 修正 review 表頭硬編碼「Claude」→「Nemotron」
- 驗證：`./twin status` 正確顯示 3 把 Key ✅；`./twin merge digital-twin v2` 正常 ✅

## 狀態
- 任務檔：`~/Projects/digital-twin/tasks/T011-unified-config.md`（done）、`T012-quality-gate-layering.md`（done）、`T024-twin-python-resolver.md`（done）
- 待執行：T013（失敗還原）、T014（自動修復迴圈）、T015（文件對齊）、T017（pyright 路徑）
