# T017 任務完成摘要（digital-twin）

- 日期：2026-08-05
- 專案：~/Projects/digital-twin（程式碼）
- 執行者：OpenCode with DeepSeek V4 Flash

## T017 - pyproject 修正 ✅

**問題**：pyright include 指向不存在的 `src/`（掃描 0 檔案，型別檢查形同虛設）、install_hooks.py import structlog 但無依賴、無 tests/（pytest 永遠 rc=5 被誤當通過）。

**變更**：

1. **pyproject.toml**：
   - `[tool.pyright] include = ["*.py", "tests/**/*.py"]`（根目錄，保守方案，不動 src/ 結構）
   - dependencies / prod / dev 三處補 `structlog>=24.1.0`
   - `[tool.ruff.lint] ignore = ["RUF001", "RUF002", "RUF003"]`（中文全形標點誤報）

2. **tests/**：新增 `test_task_parser.py`（5 個單元測試）+ `__init__.py`
   - 完整 frontmatter 解析（status/priority/assignee/fail_count）
   - 中文 + emoji 內容保留
   - 無 frontmatter 相容（回退預設）
   - 非法檔名回傳 None
   - 缺 status 欄位回退 pending
   - pytest：`5 passed`（不再 rc=5）

3. **真實 bug 修復**（pyright 掃描後發現）：
   - **P0**：`multi_ai_discuss.py` DiscussionOrchestrator 的 `template_path`/`system_prompt` 從未初始化 → 任何 `twin discuss` 執行必崩 AttributeError；已於 `__init__` 初始化（template_path 指向 templates/spec-consultation.md，不存在時用內建預設）
   - **P1**：`auto_develop.py` `_do_call_ollama(prompt, model: str = None)` 型別註記錯誤（py3.10 禁止）→ `str | None = None`
   - **P1**：OpenRouter API 回傳 `content` 可能為 None → `.get("content") or ""` 防禦式處理 + 明確錯誤

**驗證結果**：
| 項目 | 修復前 | 修復後 |
|------|--------|--------|
| pyright 掃描檔案 | 0（src/ 不存在） | 5+ 檔案（auto_develop/config/consensus_eval/install_hooks/tests） |
| pyright 真實型別錯誤 | 9（含必崩 P0） | 0（剩 5 個 reportMissingImports 為環境套件解析誤報，非程式碼問題） |
| pytest | rc=5 無測試 | 5 passed |
| ruff 錯誤數 | 486（含 384 中文誤報） | 100（E501×63 等舊債，T012 分層處理） |
| structlog import | ImportError | ✅ 可用 |

**commit**：`fbc975c` fix(T017) 已 push digital-twin

## 待辦
- T016（spec_auto_merge 假資料對照表）→ T018 → 其餘
- 剩餘 ruff 舊債（E501/F841 等 100 個）與 pyright 環境套件誤報（5 個）可另排清理任務
